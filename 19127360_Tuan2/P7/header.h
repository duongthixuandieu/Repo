#ifndef _HEADER_
#define _HEADER_

#include <iostream>
#include <fstream>
using namespace std;

bool groupSum(int start, int sum[], int target, int length);
bool splitArray(int sum[],int length);

#endif